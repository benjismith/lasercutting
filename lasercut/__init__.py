"""Shared geometry and Blender helpers for laser-cut projects.

`lasercut.panel` and `lasercut.glowforge` are pure Python and safe to import anywhere
(tests, cut-file generators). `lasercut.blender` needs `bpy` and only works inside
Blender.
"""
